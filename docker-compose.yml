version: "3.3"

services:
  app:
    build: .
    container_name: rcmltb
    command: bash start.sh
    volumes:
      - /usr/src/app/downloads:/usr/src/app/downloads:rw # optional
    ports:
      - "8090:8090" # qbittorrent selection webserver
      - "8081:8081" # rclone serve index webserver
    restart: on-failure
      
      
